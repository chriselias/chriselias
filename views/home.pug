extends ./layout.pug
include mixins/_project

block body
    .hero
        != PrismicDOM.RichText.asHtml(homepage.data.content, ctx.linkResolver)
    section
        each project in projects
            +project(project)
    